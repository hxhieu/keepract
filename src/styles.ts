import { geekblue as pallete } from '@ant-design/colors'

const primaryColour = pallete[5]
const primaryBorder = pallete[1]
const primaryBg = pallete[0]

export { primaryBorder, primaryColour, primaryBg }
